const defaultState = {
  entities: {},
  errors: {},
  fetchStatus: {}
};

export default function sortReducer(state = defaultState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
